# Applications

The `generator` and `calculator` are the two independently built applications
in the [`lab3-1` showcase](../README.md). The main README documents their roles,
builds, and end-to-end invocations.
